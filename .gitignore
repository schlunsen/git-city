node_modules/
.DS_Store
# raw image-model sheets (scripts/assets/generate.py); only the keyed PNGs in public/assets/ are committed
scripts/assets/raw/

# Written at deploy time so the live site can be matched to a commit.
# Not a dotfile: the server does not serve those, so it 404'd and the check
# that is supposed to confirm a deploy silently confirmed nothing.
public/deployed.txt
