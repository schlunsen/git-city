{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schlunsen.github.io/git-city/schema/city-config.v1.json",
  "title": "Git City config (v1)",
  "description": "Customises the Git City island generated from a GitHub profile. Publish it as .git-city/city.json in your profile repository (<login>/<login>). Data only: Git City validates every field, ignores unknown keys and never loads URLs from it. Git City is more lenient than this schema: over-long text is shortened and out-of-range numbers are clamped instead of rejected. See https://github.com/schlunsen/git-city/blob/main/docs/city-config.md",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Editor hint only; Git City never loads it."
    },
    "version": {
      "const": 1,
      "description": "Always 1 for this schema."
    },
    "island": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "maxLength": 40,
          "description": "Shown on the welcome boards and in the profile card."
        },
        "biome": {
          "enum": [
            "meadow",
            "alpine",
            "tropical",
            "savanna",
            "lakeland"
          ],
          "description": "Ground palette, relief, trees and beaches. Default: from your top language."
        },
        "shape": {
          "enum": [
            "square",
            "wide",
            "tall",
            "round",
            "plus",
            "octagon",
            "blob"
          ],
          "description": "Footprint of the paved city block."
        }
      },
      "description": "Your island: its name, ground and city shape."
    },
    "welcome": {
      "type": "string",
      "maxLength": 120,
      "description": "Message on the welcome boards where the roads leave town."
    },
    "look": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "accent": {
          "$ref": "#/$defs/color",
          "description": "Glow colour of the plaza monument and its inlays."
        },
        "time": {
          "enum": [
            "auto",
            "day",
            "sunset",
            "night",
            "cycle"
          ],
          "description": "Starting light; visitors can still change it."
        },
        "weather": {
          "enum": [
            "clear",
            "rain",
            "snow"
          ],
          "description": "Starting weather; visitors can still change it."
        },
        "tv": {
          "type": "boolean",
          "description": "Old-television (CRT) look on or off when your city opens; visitors can still toggle it."
        },
        "fx": {
          "type": "boolean",
          "description": "Bloom / colour grade / grain on or off when your city opens; visitors can still toggle it."
        },
        "timezone": {
          "type": "string",
          "maxLength": 64,
          "pattern": "^[A-Za-z][A-Za-z0-9_+\\-/]{0,63}$",
          "description": "Your IANA time zone, e.g. \"Europe/Copenhagen\" or \"Asia/Tokyo\": the city's automatic day and night follow your local time."
        }
      },
      "description": "How your city opens. Visitors can still change the light, weather, TV and FX during a visit."
    },
    "landmarks": {
      "type": "array",
      "maxItems": 6,
      "uniqueItems": true,
      "items": {
        "enum": [
          "rollerCoaster",
          "carousel",
          "circusTent",
          "dropTower",
          "windTurbines",
          "windmill",
          "farm",
          "campsite",
          "radioTower",
          "observatory",
          "balloonPad",
          "lighthouse",
          "recordShop",
          "robotMonument"
        ]
      },
      "description": "Attractions placed first (best effort: each needs a free site of the right kind)."
    },
    "neighbours": {
      "type": "array",
      "maxItems": 6,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/login"
      },
      "description": "GitHub logins reached by flying off the map; topped up automatically."
    },
    "featured": {
      "type": "array",
      "maxItems": 12,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/repo"
      },
      "description": "Your repositories, in order: built next to the plaza and advertised first on the country billboards."
    },
    "hide": {
      "type": "array",
      "maxItems": 50,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/repo"
      },
      "description": "Your repositories to leave out of the city."
    },
    "repos": {
      "type": "object",
      "maxProperties": 100,
      "propertyNames": {
        "$ref": "#/$defs/repo"
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "style": {
            "enum": [
              "auto",
              "tower",
              "stepped",
              "cottage",
              "block"
            ],
            "description": "Silhouette: tower with a spire, stepped tower, cottage with a pitched roof, or a plain block. Height still follows the stars."
          },
          "color": {
            "$ref": "#/$defs/color",
            "description": "Facade colour (instead of the language colour)."
          },
          "sign": {
            "type": "string",
            "maxLength": 40,
            "description": "Text on the building's shop signs and rooftop board."
          },
          "billboard": {
            "type": "string",
            "maxLength": 120,
            "description": "Text on this repository's country billboard."
          },
          "graffiti": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "text"
            ],
            "properties": {
              "text": {
                "type": "string",
                "minLength": 1,
                "maxLength": 60,
                "description": "The words: free-form, up to 60 characters."
              },
              "color": {
                "$ref": "#/$defs/color",
                "description": "Paint colour (default depends on the style)."
              },
              "style": {
                "enum": [
                  "tag",
                  "bubble",
                  "stencil"
                ],
                "description": "A quick marker tag, fat bubble letters, or a stencil. Default: tag."
              }
            },
            "description": "Spray-painted on the ground-floor walls."
          },
          "roof": {
            "enum": [
              "auto",
              "garden",
              "helipad",
              "solar",
              "pool",
              "antenna",
              "none"
            ],
            "description": "What's on the (flat) roof."
          },
          "neon": {
            "$ref": "#/$defs/color",
            "description": "Night glow colour of the lit windows."
          },
          "flag": {
            "type": "string",
            "maxLength": 16,
            "description": "An emoji or up to 3 characters on a small rooftop flag."
          }
        }
      },
      "description": "Per-building settings, keyed by your repository names. The same fields as a repository's own .git-city/building.json; these win, field by field."
    },
    "player": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "music": {
          "enum": [
            "floating-cities",
            "deliberate-thought",
            "cipher",
            "digital-lemonade",
            "crypto",
            "none"
          ],
          "description": "Track (music by Kevin MacLeod, CC BY 4.0), or none."
        },
        "volume": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "description": "Music volume, 0 to 100."
        }
      },
      "description": "Soundtrack of the in-page Gource View player."
    },
    "plane": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "color": {
          "$ref": "#/$defs/color",
          "description": "Fuselage and tail colour of the explore-mode biplane."
        },
        "name": {
          "type": "string",
          "maxLength": 24,
          "description": "Painted on both sides of the fuselage."
        }
      },
      "description": "Your biplane in the explore fly mode."
    }
  },
  "$defs": {
    "color": {
      "type": "string",
      "pattern": "^#[0-9a-fA-F]{6}$"
    },
    "login": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9](?:[a-zA-Z0-9]|-(?=[a-zA-Z0-9])){0,38}$"
    },
    "repo": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._-]{1,100}$"
    }
  }
}
